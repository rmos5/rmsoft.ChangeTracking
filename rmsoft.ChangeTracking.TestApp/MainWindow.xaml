<Window x:Class="rmsoft.ChangeTracking.TestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:rmsoft.ChangeTracking.TestApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        d:DataContext="{d:DesignData Type=local:MainContext,IsDesignTimeCreatable=False}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Data">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <WrapPanel Orientation="Horizontal">
                        <Button Content="Get Data" Command="{Binding GetDataCommand}"/>
                        <ToggleButton Content="Edit" Command="{Binding ToggleDataEditCommand}"/>
                        <Button Content="Add" Command="{Binding AddDataCommand}"/>
                        <Button Content="Remove" Command="{Binding RemoveDataCommand}"/>
                        <Button Content="Undo" Command="{Binding UndoDataChangesCommand}"/>
                        <Button Content="Redo" Command="{Binding RedoDataChangesCommand}"/>
                        <Button Content="Apply" Command="{Binding ApplyDataChangesCommand}"/>
                        <CheckBox Content="Append data" IsChecked="{Binding IsAppendData,Mode=TwoWay}"/>
                    </WrapPanel>
                    
                    <ListBox Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}" d:SelectedIndex="3" ItemsSource="{Binding}" SelectedItem="{Binding SelectedItem,Mode=TwoWay}">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:NamedObject}">
                                <StackPanel Orientation="Horizontal" x:Name="stackPanel1">
                                    <TextBlock Text="{Binding Name}" x:Name="textBlock1"/>
                                </StackPanel>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsTracking}" Value="True">
                                        <Setter TargetName="stackPanel1" Property="Background" Value="LightGreen"/>
                                        <Setter TargetName="textBlock1" Property="FontWeight" Value="SemiBold"/>
                                        <Setter TargetName="textBlock1" Property="FontSize" Value="14"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Item">
                <Grid IsEnabled="{Binding HasSelectedItem}">
                    <Grid DataContext="{Binding SelectedItem, Mode=OneWay}" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <ToggleButton Content="Edit" Command="{Binding ToggleEditingCommand}" IsChecked="{Binding IsTracking, Mode=OneWay}"/>
                            <Button Content="Undo" Command="{Binding UndoChangesCommand}"/>
                            <Button Content="Redo" Command="{Binding RedoChangesCommand}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" HorizontalAlignment="Left">
                            
                            <StackPanel.Resources>
                                <Style TargetType="TextBox">
                                    <Setter Property="IsReadOnlyCaretVisible" Value="True"/>
                                    <Setter Property="IsReadOnly" Value="True"/>
                                    <Setter Property="Margin" Value="2"/>
                                    <Setter Property="Padding" Value="2"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTracking}" Value="True">
                                            <Setter Property="IsReadOnly" Value="False"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsChanged}" Value="True">
                                            <Setter Property="BorderBrush" Value="LightGreen"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Resources>
                            
                            <TextBox Text="{Binding Name, Mode=TwoWay}"/>
                            <TextBox Text="{Binding Description, Mode=TwoWay}"/>
                            <Button Margin="0,5,0,0" Content="Apply" Command="{Binding ApplyChangesCommand}"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>

</Window>

<Window x:Class="rmsoft.ChangeTracking.TestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:rmsoft.ChangeTracking.TestApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        d:DataContext="{d:DesignData Type=local:MainContext,IsDesignTimeCreatable=False}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Data">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ListBox d:ItemsSource="{d:SampleData ItemCount=5}" d:SelectedIndex="3" ItemsSource="{Binding}" SelectedItem="{Binding SelectedItem,Mode=TwoWay}">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:NamedObject}">
                                <StackPanel Orientation="Horizontal" x:Name="stackPanel1">
                                    <TextBlock Text="{Binding Name}" x:Name="textBlock1"/>
                                </StackPanel>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding HasChanges}" Value="True">
                                        <Setter TargetName="stackPanel1" Property="Background" Value="LightGreen"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsTracking}" Value="True">
                                        <Setter TargetName="textBlock1" Property="FontWeight" Value="SemiBold"/>
                                        <Setter TargetName="textBlock1" Property="FontSize" Value="14"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <StackPanel Grid.Column="1">
                        <CheckBox Content="Insert data" IsChecked="{Binding IsInsertData,Mode=TwoWay}"/>
                        <Button Content="Get Data" Command="{Binding GetDataCommand}"/>
                        <CheckBox Content="Edit" Command="{Binding ToggleTrackingCommand}" IsChecked="{Binding IsTracking, Mode=OneWay}"/>
                        <Button Content="New" Command="{Binding AddNewDataCommand}"/>
                        <Button Content="Remove" Command="{Binding RemoveItemCommand}"/>
                        <Button Content="Move top" Command="{Binding MoveItemCommand}" CommandParameter="0"/>
                        <Button Content="Replace top" Command="{Binding ReplaceItemCommand}" CommandParameter="0"/>
                        <Button Content="Clear" Command="{Binding ClearItemsCommand}"/>
                        <Button Content="Undo" Command="{Binding UndoChangesCommand}"/>
                        <Button Content="Redo" Command="{Binding RedoChangesCommand}"/>
                        <Button Content="Apply" Command="{Binding ApplyChangesCommand}"/>

                    </StackPanel>
                </Grid>

            </GroupBox>

            <GroupBox Grid.Column="1" Header="Item">
                <Grid IsEnabled="{Binding HasSelectedItem}">
                    <Grid DataContext="{Binding SelectedItem, Mode=OneWay}" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <CheckBox Content="Edit" Command="{Binding ToggleTrackingCommand}" IsChecked="{Binding IsTracking, Mode=OneWay}"/>
                            <Button Content="Undo" Command="{Binding UndoChangesCommand}"/>
                            <Button Content="Redo" Command="{Binding RedoChangesCommand}"/>
                            <Button Content="Apply" Command="{Binding ApplyChangesCommand}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">

                            <StackPanel.Resources>
                                <Style TargetType="TextBox">
                                    <Setter Property="IsReadOnlyCaretVisible" Value="True"/>
                                    <Setter Property="IsReadOnly" Value="True"/>
                                    <Setter Property="Margin" Value="2"/>
                                    <Setter Property="Padding" Value="2"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTracking}" Value="True">
                                            <Setter Property="IsReadOnly" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Resources>

                            <TextBox Text="{Binding Name, Mode=TwoWay}"/>
                            <TextBox Text="{Binding Description, Mode=TwoWay}"/>

                        </StackPanel>
                    </Grid>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>

</Window>
